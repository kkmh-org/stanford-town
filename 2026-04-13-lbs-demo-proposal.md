# 角色世界模拟器 Demo 方案

> 日期：2026-04-13
>
> 对齐：战役二 O3 — 支撑 LBS 世界，角色世界模拟器的系统定义，及角色日常编剧 Agent 实现
>
> 目标：向老板展示 Generative Agents 技术框架作为"角色世界模拟器"的可行性

---

## 一、OKR 对齐

### O3 的 5 个 KR 与 Generative Agents 的映射

```
O3 KR                              Generative Agents 已实现              Demo 要展示
─────                              ────────────────────                  ──────────
KR1 角色世界日常状态模拟              reverie.py 主循环                     角色在跑，每个tick有状态变化
                                    每tick: perceive→retrieve→plan
                                    →reflect→execute

KR2 角色日常结构化描述                scratch.py                           角色有完整的日程输出
    工作日/假日/偶发事件              - daily_req (今天目标)                 工作日 vs 假日不同
                                    - f_daily_schedule (分钟级日程)
                                    - f_daily_schedule_hourly_org

KR3 角色日常流转路径                  plan.py                              角色在地点间移动的轨迹
    个性化 + Harness                - daily_plan → hourly → task_decomp    不同角色不同路线
                                    - action_sector → arena → object      Harness = 内容同学提供的约束
                                    spatial_memory.py (知道哪些地点)

KR4 角色活动轨迹 → LBS              execute.py                            轨迹数据输出到前端
    推动角色自进化                    - 寻路 + 位置更新                      reflect → 新 thought 写回记忆
                                    reflect.py
                                    - 反思生成洞察 → 写回记忆流

KR5 角色日常影响 deepfeeling         perceive.py                           角色日常中的事件有情感权重
                                    - poignancy 打分 (1-10)               累积到阈值触发反思
                                    - importance_trigger 累积              为 deepfeeling VAD 提供输入
```

---

## 二、Demo 要证明的三件事

### 1. 角色能自主生成结构化日程（KR2）

**展示**：给模拟器一个角色设定，自动生成一天的日程

```
输入：
  角色：钟辰时
  身份：某公司高管，控制型性格
  生活习惯：早起，7点到公司，晚上10点下班
  今天是：工作日

输出（自动生成）：
  07:00 起床，洗漱                    ← wake_up_hour
  07:30 出门，去公司                   ← daily_plan
  08:00 到公司，查看邮件 (30min)       ← task_decomp
  08:30 开部门周会 (60min)
  09:30 处理项目审批 (90min)
  11:00 与合作方电话会议 (30min)
  11:30 午餐前处理紧急事务 (30min)
  12:00 在公司附近餐厅吃午餐 (60min)   ← action_sector → action_arena
  13:00 午休 (30min)
  13:30 下午工作 (180min)
  16:30 健身房运动 (60min)
  17:30 回公司处理收尾工作 (150min)
  20:00 下班，回家
  21:00 在家看书 (60min)
  22:00 睡觉
```

**对比**：换一个角色（周往），同样的日期，生成完全不同的日程——因为人格和生活方式不同。

### 2. 角色按日程在地点间移动，形成轨迹（KR3 + KR4）

**展示**：把日程投射到地图 POI 上，生成角色一天的移动轨迹

```
时间轴：
  07:00 ──── 家
  07:30 ──── 家 → 公司（移动中）
  08:00 ──── 公司·办公室
  12:00 ──── 公司 → 附近餐厅（移动中）
  12:30 ──── 餐厅
  13:00 ──── 餐厅 → 公司
  16:30 ──── 公司 → 健身房
  17:30 ──── 健身房 → 公司
  20:00 ──── 公司 → 家
  22:00 ──── 家（睡觉）
```

**关键**：这个轨迹是 **LLM 基于角色设定自动生成的**，不是人工编排的脚本。

### 3. 角色在日常中积累记忆和触发反思（KR4 + KR5）

**展示**：模拟器跑一天后，角色的记忆流和反思输出

```
记忆流（自动积累）：
  [event] 钟辰时 在办公室 处理邮件            poignancy: 2
  [event] 钟辰时 参加部门周会                 poignancy: 3
  [event] 钟辰时 与合作方通话讨论项目          poignancy: 5
  [event] 钟辰时 在餐厅遇到了周往             poignancy: 6  ← 偶发事件
  [event] 钟辰时 在健身房锻炼                 poignancy: 2
  [chat]  钟辰时 和周往 聊了工作压力           poignancy: 7  ← 社交互动

反思（importance 累积到阈值后触发）：
  [thought] 钟辰时觉得最近工作压力很大，需要找时间放松
  [thought] 钟辰时注意到周往今天心情不太好，可能跟项目有关
    → 这些 thought 会影响明天的日程生成
    → 这些 thought 可以作为 deepfeeling 的情感输入
```

---

## 三、Demo 实现方案

### 3.1 最简方案：命令行 Demo（1 天）

不做前端，直接用改造后的 Generative Agents 跑，输出结构化日志。

```python
# demo_lbs.py — 角色世界模拟器 Demo

# 1. 初始化角色（替换 Isabella → 钟辰时）
# 2. 运行 1 天模拟（144 个 tick，每 tick 10 分钟）
# 3. 输出：
#    - 日程表（f_daily_schedule）
#    - 位置轨迹（每个 tick 的 act_address）
#    - 记忆流（所有 event/thought/chat 节点）
#    - 反思记录（triggered reflections）
```

**优点**：最快出结果，直接证明技术可行性
**缺点**：没有视觉冲击，需要老板看日志

### 3.2 推荐方案：简化地图 + 时间轴可视化（3 天）

做一个简单的 HTML 页面，用时间轴 + 地图展示角色一天的生活。

```
┌─────────────────────────────────────────────────────────┐
│  角色世界模拟器 Demo                          [▶ 开始模拟] │
│                                                         │
│  ┌──────────┐  ┌──────────────────────────────────────┐ │
│  │          │  │                                      │ │
│  │  简化地图  │  │        时间轴 + 活动流                │ │
│  │          │  │                                      │ │
│  │  🏠 家    │  │  08:00  📧 查看邮件 @公司·办公室      │ │
│  │          │  │  08:30  👥 部门周会 @公司·会议室       │ │
│  │  🏢 公司  │  │  12:00  🍜 午餐 @附近餐厅             │ │
│  │          │  │  12:30  💬 遇到周往，聊了工作压力       │ │
│  │  🍜 餐厅  │  │  16:30  🏋️ 健身 @健身房              │ │
│  │          │  │  20:00  🏠 回家                       │ │
│  │  🏋️ 健身房│  │                                      │ │
│  │          │  │  ─── 反思 ───                         │ │
│  │  🌳 公园  │  │  💭 最近工作压力大，需要找时间放松      │ │
│  │          │  │  💭 周往今天心情不太好                   │ │
│  └──────────┘  └──────────────────────────────────────┘ │
│                                                         │
│  ┌──────────────────────────────────────────────────────┐│
│  │  记忆流 (12 events, 2 thoughts, 1 chat)              ││
│  │  ██████████████████████░░░░░ importance: 127/150     ││
│  └──────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────┘
```

**技术栈**：
- 后端：改造后的 Generative Agents（已完成），新写一个 `demo_lbs.py` 驱动模拟
- 前端：一个静态 HTML + JS，轮询后端输出的 JSON
- 不需要 Django，不需要原版的方格地图

### 3.3 具体步骤

| 步骤 | 内容 | 产出 |
|------|------|------|
| 1 | **创建角色数据**：用钟辰时/周往/沈佑的人格设定替换 Isabella/Klaus/Maria 的 scratch.json | 3 个角色的 bootstrap_memory |
| 2 | **定义 LBS 世界空间**：用 POI 列表替换 spatial_memory.json（公司、餐厅、健身房、公园、家...） | spatial_memory.json |
| 3 | **写 demo 驱动脚本**：不走 Django 前端，直接调 Persona.move() 循环，输出 JSON 日志 | demo_lbs.py |
| 4 | **做可视化页面**：HTML + JS 读取 JSON，渲染时间轴 + 简化地图 + 记忆流 | demo.html |
| 5 | **跑 3 个角色 1 天**：生成完整日志，截图/录屏 | 演示材料 |

---

## 四、给老板讲的故事

### 一句话

> "我们用斯坦福的 Generative Agents 认知架构，改造出了一个**角色世界模拟器**——给它一个角色设定，它能自动生成这个角色一天的日程、轨迹、记忆和反思，直接输出到 LBS 世界。"

### 三层递进

**第一层（KR2 已验证）**：
"给模拟器一个角色设定，它能自动生成结构化日程——工作日和假日不一样，每个角色的日程由他的人格决定。"

**第二层（KR3 已验证）**：
"这个日程不只是文字，它会映射到 LBS 地图上的 POI——角色在什么时间去什么地方，形成移动轨迹。不是我们写的脚本，是 LLM 根据角色设定自动规划的。"

**第三层（KR4+KR5 已验证）**：
"角色在一天中积累记忆、产生反思，这些反思会影响明天的日程。角色会'进化'——今天遇到了一件重要的事，明天的行为就会不一样。而且这些事件都带有情感权重，可以直接对接 deepfeeling 的情绪模型。"

### 对齐战略

> "这不是一个技术玩具。它是 O3 的技术底座——角色世界模拟器。日程生成、轨迹规划、记忆积累、反思进化，这四件事合在一起，就是 LBS 世界里'活的角色'。"

---

## 五、Demo 之后的路径

```
Demo (3天)                   KR 交付                        产品集成
────────                   ──────────                      ──────────
3 角色 × 1 天模拟            完整的日程结构化定义（KR2）         接入真实 LBS 地图
POI 点位 + 时间轴            流转路径设计文档（KR3）            多角色并行模拟
命令行/简单可视化             偶发事件机制设计                    与故事模式编剧Agent联动
记忆流 + 反思输出             自进化机制文档（KR4）              deepfeeling VAD 对接（KR5）
                            deepfeeling 输入定义（KR5）        Harness（内容同学约束）接入
```
