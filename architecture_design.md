# NPC Agent 系统架构设计文档

> 2026-04-15 · 基于 generative_agents（斯坦福小镇）研究 + 团队战役二 OKR 讨论

---

## 一、系统总览

### 1.1 设计目标

让 AI 角色具备**灵魂感**（主体性 + 进化）：
- **有故事**：编剧 Agent 推动剧情，角色活在故事里
- **有主体**：角色通过 Deep Feeling 自主感受、推理、决策，有能力偏离编剧安排的轨道
- **有进化**：角色的情感、关系、认知随经历持续变化

### 1.2 核心组件

| 组件 | 角色 | 类比 |
|------|------|------|
| 时钟 | 驱动一切的心跳 | 时间 |
| 天道 Agent | 独立运行，产出不可抗力事件和世界大势 | 大运流年 / 自然法则 |
| 编剧 Agent | 产出有叙事意图的事件 + 角色日程 + 剧情指令 | 编剧 / 导演 |
| 世界模拟 Agent | 维护世界状态，分发变更通知 | 物理世界 |
| 角色 Agent | 感知、感受、决策、行动的主体 | 人 |

### 1.3 系统拓扑（数据流）

```
时钟
  │
  ├──→ 天道 Agent
  │      │ common_events (不可抗力) ──→ 世界模拟 Agent
  │      │ context (世界大势) ─────────→ 编剧 Agent
  │
  └──→ 世界模拟 Agent（时钟驱动的机械更新：课表、作息）
  
编剧 Agent
  │ common_events (有意图的世界事件) ──→ 世界模拟 Agent
  │ persona_events (角色相关世界事件) ─→ 世界模拟 Agent
  │ plot_directive (剧情指令) ──────────→ 角色 Agent（直达）
  
世界模拟 Agent
  │ world_state ────→ 编剧 Agent（编剧决策依据）
  │ event_info[] ──→ 角色 Agent（按 item tree 过滤分发）
  
角色 Agent
  │ action_results ─→ 世界模拟 Agent（行为导致的物品状态变更）
  │ character_state ─→ 编剧 Agent（情感/关系/位置/成长）
  │ reply ──────────→ 用户

用户
  │ user_input ────→ 世界模拟 Agent（直接操作世界）
  │ user_message ──→ 角色 Agent（聊天互动）
```

### 1.4 完整数据流清单

| # | 从 | 到 | 数据 | 说明 |
|---|----|----|------|------|
| 1 | 时钟 | 天道 | `tick` `current_time` | 驱动天道演进 |
| 2 | 时钟 | 世界模拟 | `tick` `current_time` | 驱动机械更新（课表、作息） |
| 3 | 天道 | 编剧 | `context` | 世界大势/氛围 |
| 4 | 天道 | 世界模拟 | `common_events[]` | 不可抗力事件（天气、灾害） |
| 5 | 编剧 | 世界模拟 | `common_events[]` `persona_events[]` | 有意图的世界事件 + 角色相关世界事件 |
| 6 | 编剧 | 角色 | `plot_directive` | 剧情指令：直接告诉角色接下来的剧情内容 |
| 7 | 世界模拟 | 编剧 | `world_state` | 当前物品状态，编剧决策依据 |
| 8 | 世界模拟 | 角色 | `event_info[]` | 按角色 item tree 过滤分发 |
| 9 | 角色 | 世界模拟 | `action_results[]` | 行为导致的物品状态变更 |
| 10 | 角色 | 编剧 | `character_state` | 情感/关系/位置/成长 |
| 11 | 用户 | 世界模拟 | `user_input` | 用户直接操作世界 |
| 12 | 用户 | 角色 | `user_message` | 用户跟角色聊天 |
| 13 | 角色 | 用户 | `reply` | 角色回复用户 |

---

## 二、各 Agent I/O 定义

### 2.1 时钟

| 方向 | 字段 | 说明 |
|------|------|------|
| out | `tick` | 心跳信号 |
| out | `current_time` | 当前模拟时间 |

### 2.2 天道 Agent

独立运行，不受任何其他 Agent 影响。像自然法则一样，产出不可抗力的世界事件。

| 方向 | 字段 | 说明 | 来源/下游 |
|------|------|------|----------|
| in | `tick`, `current_time` | 时钟驱动 | ← 时钟 |
| out | `common_events[]` | 不可抗力事件：天气变化、自然灾害等 | → 世界模拟 |
| out | `context` | 世界大势/氛围描述：联赛季、雨季等 | → 编剧 |

**context 示例：**
```json
{
  "current_phase": "篮球联赛备战期",
  "world_mood": "紧张、竞争",
  "season": "晚春、多雨",
  "active_macro_events": ["校际篮球联赛下周开始", "期中考试临近"],
  "narrative_tension": 0.7
}
```

### 2.3 编剧 Agent

有叙事意图的事件生产者。两种模式：故事模式（用户进入角色世界）和日常模式（角色来到用户世界）。

| 方向 | 字段 | 说明 | 来源/下游 |
|------|------|------|----------|
| in | `天道.context` | 世界大势 | ← 天道 |
| in | `world_state` | 当前世界物品状态 + 时间 | ← 世界模拟 |
| in | `character_states[]` | 各角色情感/关系/位置 | ← 角色 Agent |
| in | `user_behavior` | 用户近期行为 | ← 角色 Agent（用户交互记录） |
| in | `plot_progress` | 剧情进度、活跃线索 | 自身维护 |
| in | `story_config` | 作品设定、harness | 静态配置 |
| out | `common_events[]` | 有意图的世界事件（如"南湾市有演唱会"） | → 世界模拟 |
| out | `persona_events[]` | 角色相关世界事件 | → 世界模拟 |
| out | `plot_directive` | 剧情指令 + 角色日程安排 | → 角色 Agent |

**编剧产出的 plot_directive 包含两类内容：**

1. **日程安排**（默认轨道）：角色今天的日程表，角色可以基于 deepfeeling 偏离
2. **剧情事件**：接下来要发生的剧情内容，角色通过感知获取

**两种模式的差异：**

| | 故事模式 | 日常陪伴模式 |
|---|---------|-----------|
| 世界观 | 角色的世界（南一高中） | 用户的真实世界 |
| 编剧产出 | 推动角色世界故事线的事件 | 角色"那边"发生了什么 |
| 用户角色 | 用户是世界中的参与者 | 用户是"另一个世界的人" |
| 交互方式 | 角色与用户在同一场景 | 聊天、打电话、发消息（异步） |

### 2.4 世界模拟 Agent

纯粹的状态管理器。不产出事件，只处理和分发。

| 方向 | 字段 | 说明 | 来源/下游 |
|------|------|------|----------|
| in | `天道.common_events[]` | 不可抗力事件 | ← 天道 |
| in | `编剧.common_events[]` | 有意图的世界事件 | ← 编剧 |
| in | `编剧.persona_events[]` | 角色相关世界事件 | ← 编剧 |
| in | `tick`, `current_time` | 机械更新：课表、作息 | ← 时钟 |
| in | `action_results[]` | 角色行为导致的物品变更 | ← 角色 Agent |
| in | `user_input` | 用户直接操作世界 | ← 用户 |
| internal | `item_state_tree` | 全量物品状态树 | — |
| out | `world_state` | 更新后的世界状态快照 | → 编剧 |
| out | `event_info[]` | 物品变更通知，按角色 item tree 分发 | → 角色 Agent |

**全量物品状态树示例：**
```json
{
  "南一高中": {
    "教学楼": {
      "高二三班教室": {
        "钟辰时的书桌": {
          "state": "整齐摆放着课本和笔记",
          "owner": "钟辰时",
          "last_changed": "2026-04-15 08:30"
        },
        "黑板": {
          "state": "写着今天的值日表",
          "owner": null,
          "last_changed": "2026-04-15 07:50"
        }
      }
    }
  }
}
```

**event_info 示例：**
```json
{
  "item": "南一高中:教学楼:高二三班教室:钟辰时的书桌",
  "old_state": "整齐摆放着课本和笔记",
  "new_state": "课本散落一地，椅子倒了",
  "cause": "周往路过时不小心撞到",
  "time": "2026-04-15 10:15",
  "related_personas": ["钟辰时", "周往"]
}
```

### 2.5 角色 Agent

感知、感受、决策、行动的主体。详见第三章。

| 方向 | 字段 | 说明 | 来源/下游 |
|------|------|------|----------|
| in | `event_info[]` | 与自己相关的物品状态变更 | ← 世界模拟 |
| in | `plot_directive` | 剧情指令 + 日程安排 | ← 编剧 |
| in | `user_message` | 用户发来的消息 | ← 用户 |
| internal | Soul Agent (Deep Feeling) | 情感推理引擎 | — |
| internal | 记忆 (Memory) | 记忆流系统 | — |
| internal | 情绪 (Emotion/VAD) | 情绪状态系统 | — |
| internal | Item Tree | 角色关联物品子集 | — |
| internal | Skills | 通用技能 + 角色专属技能 | — |
| out | `action_results[]` | 行为导致的物品状态变更 | → 世界模拟 |
| out | `character_state` | 情感/关系/位置/成长 | → 编剧 |
| out | `reply` | 对用户的回复 | → 用户 |

---

## 三、角色 Agent 内部架构

### 3.1 设计原则

1. **角色是"活人"**：主动感知环境，不是被动接收通知
2. **记忆和情绪是两套独立系统**：记忆 = 经历了什么；情绪 = 当下的感受（VAD）
3. **用 Deep Feeling 替代论文的反思模块**：不只是"想明白了什么"，而是"经历了什么样的内心过程"
4. **编剧给默认轨道，角色保留主体性**：日程由编剧安排，角色可以基于 deepfeeling 偏离
5. **偏离 = 主体性的体现**：灵魂感 = 主体性 + 进化

### 3.2 三种模式：Short / Brief / Deep

角色不是对所有感知都做深度推理。大部分时候直接按日程执行，不需要任何内心戏。

| 模式 | 触发条件 | 做什么 | 占比 | 举例 |
|------|---------|--------|------|------|
| **Short** | 日常琐事，无情感波动 | 不推理，直接按日程执行 | ~80% | 上课铃响→去上课，吃饭时间→去食堂 |
| **Brief** | 轻微情感触动 | 简短推理，一行内心，不改变行为 | ~15% | 看到周往在打球→"又在练球了"，继续按日程走 |
| **Deep** | 强烈情感冲击，角色内心经历"不止一种可能性" | 完整推理+反思+多方案权衡，可能偏离日程 | ~5% | 周往受伤→内心挣扎→决定不去上课先去看他 |

**只有 Deep 模式才可能触发日程偏离，Brief 和 Short 都按日程执行。**

### 3.3 内部结构

```
角色 Agent
┌──────────────────────────────────────────────────────┐
│                                                      │
│  ┌───────────────────────────────────┐               │
│  │          感知 Perceive             │               │
│  │  主动扫描，统一入口                  │               │
│  │                                    │               │
│  │  · 日程安排（编剧产出的默认轨道）     │               │
│  │  · 物品状态变化（世界模拟推送）       │               │
│  │  · 剧情内容（编剧产出）              │               │
│  │  · 其他角色的行为和对话              │               │
│  │  · 用户消息                         │               │
│  │  · 环境变化（天气、时间等）           │               │
│  └──────────────┬────────────────────┘               │
│                 │                                    │
│                 ▼                                    │
│  ┌──────────────────────────────────┐                │
│  │     记忆 Memory    │   情绪 VAD   │                │
│  │                    │              │                │
│  │  · 观察类记忆       │  · 当前 VAD  │                │
│  │  · 反思类记忆       │  · 变化记录  │                │
│  │  · 计划类记忆       │  · 衰减规则  │                │
│  │                    │              │                │
│  │  三维检索：          │  感知写入时   │                │
│  │  recency +         │  同步更新     │                │
│  │  importance +      │              │                │
│  │  relevance         │              │                │
│  │                    │              │                │
│  │     ◄── 互相影响 ──►             │                │
│  └──────────────┬───────────────────┘                │
│                 │                                    │
│                 ▼                                    │
│  ┌──────────────────────────────────┐                │
│  │        模式分流 Mode Router        │                │
│  │                                   │                │
│  │  intent_skill 判断意图 + 情感强度    │                │
│  │                                   │                │
│  │  ┌─────┐  ┌─────┐  ┌──────┐     │                │
│  │  │Short│  │Brief│  │ Deep │     │                │
│  │  │     │  │     │  │      │     │                │
│  │  │不推理│  │简短  │  │完整   │     │                │
│  │  │直出  │  │推理  │  │推理   │     │                │
│  │  │     │  │     │  │+反思  │     │                │
│  │  │     │  │     │  │+权衡  │     │                │
│  │  └──┬──┘  └──┬──┘  └──┬───┘     │                │
│  │     │        │        │         │                │
│  │     │   一行内心  多方案权衡       │                │
│  │     │   VAD微调   VAD大幅变化      │                │
│  │     │        │    行为意图         │                │
│  │     │        │    (按日程or偏离)   │                │
│  └─────┼────────┼────────┼──────────┘                │
│        │        │        │                           │
│        ▼        ▼        ▼                           │
│  ┌──────────────────────────────────┐                │
│  │          执行 Execute              │                │
│  │                                   │                │
│  │  Short/Brief → 按日程执行           │                │
│  │  Deep + 强情感 → 可能偏离日程        │                │
│  │                                   │                │
│  │  · action_results → 世界模拟       │                │
│  │  · reply → 用户                    │                │
│  │  · character_state → 编剧          │                │
│  └──────────────────────────────────┘                │
│                                                      │
│  ┌──────────────────────────────────┐                │
│  │  Item Tree    │    Skills         │                │
│  │               │                   │                │
│  │  角色关联物品   │  通用技能          │                │
│  │  的子集        │  + 角色专属技能     │                │
│  └──────────────────────────────────┘                │
│                                                      │
└──────────────────────────────────────────────────────┘
```

### 3.4 每 tick 循环

```
每个 tick：

1. 感知（Perceive）
   主动扫描当前能获取的所有信息
   
2. 记忆 + 情绪更新
   感知到的内容写入记忆流（观察类记忆）
   同时更新 VAD 情绪状态
   
3. 模式分流（Mode Router）
   intent_skill 判断意图和情感强度：
   
   · 无情感波动 → Short：跳过推理，直接按日程执行
   · 轻微触动   → Brief：简短推理，一行内心，按日程执行
   · 强烈冲击   → Deep：完整推理+反思+多方案权衡
   
4. 执行（Execute）
   Short / Brief → 按日程执行
   Deep + 强情感 → 可能偏离日程（偏离 = 主体性）
   产出 action_results / reply / character_state
   
5. 异步更新
   emotion_update：写入新的 VAD 状态
   memory_write：如有必要，写入记忆摘要
```

### 3.5 与斯坦福小镇论文的关键差异

| 环节 | 论文（斯坦福小镇） | 我们的设计 |
|------|-----------------|----------|
| 感知 | pull，扫描附近 tile 的 events | pull，感知源更丰富（日程 + 物品变化 + 剧情 + 用户 + 其他角色 + 环境） |
| 记忆 | 记忆流（观察/反思/计划，8字段），是唯一系统 | **保留记忆流，但与情绪分离为两套独立系统** |
| 情绪 | 没有 | **新增 VAD 情绪系统**，独立存储，与记忆互相影响 |
| 推理/反思 | 简单的 LLM 推理 + 重要性>150 触发反思 | **替换为 Deep Feeling 完整链路**：intent→mode→信息装载→reasoning→reflection |
| 规划 | 角色自主三级规划（粗→小时→执行级） | **日程由编剧 Agent 安排**，角色感知日程作为默认轨道 |
| 主体性 | 完全自主（但缺乏叙事控制） | **编剧给轨道 + 角色可偏离**，偏离由 deepfeeling 驱动 |
| 执行 | 移动 + 物品操作 | 保留，加上回复用户、状态反馈给编剧 |

### 3.6 记忆系统设计（参考论文）

**单条记忆的数据结构（8字段，与论文一致）：**

| 字段 | 说明 |
|------|------|
| 记忆 ID | 唯一主键 |
| 自然语言描述 | 记忆内容，全程自然语言 |
| 记忆类型 | 观察 / 反思 / 计划 |
| 创建时间戳 | 记忆写入的精确游戏时间 |
| 最近访问时间戳 | 被检索召回时实时更新 |
| 重要性分数 | 1-10，LLM 生成，终身不变 |
| 引用指针列表 | 反思/计划类必填，溯源 |
| 嵌入向量 | 用于相关性检索 |

**三维检索机制（与论文一致）：**

$$\text{Final Score} = \text{Normalized Recency} + \text{Normalized Importance} + \text{Normalized Relevance}$$

- 新近度：指数衰减，基数 0.995，单位游戏小时
- 重要性：LLM 生成的 1-10 分
- 相关性：嵌入向量余弦相似度

### 3.7 情绪系统设计（新增）

独立于记忆系统，用 VAD 模型表示角色当下的情绪状态：

| 维度 | 含义 | 范围 |
|------|------|------|
| V (Valence) | 愉悦度 | -1 ~ 1 |
| A (Arousal) | 激活度 | -1 ~ 1 |
| D (Dominance) | 支配感 | -1 ~ 1 |

- 每次感知更新时，同步更新 VAD
- VAD 变化由 deepfeeling 的 reasoning_skill 产出
- VAD 随时间自然衰减（回归角色基线）
- VAD 状态作为 deepfeeling 的输入（emotion_skill 读取）

### 3.8 角色 Item Tree

每个角色拥有自己关联的物品子集，是世界全量物品状态树的子视图。

```json
// 钟辰时的 item tree
{
  "南一高中": {
    "教学楼": {
      "高二三班教室": ["钟辰时的书桌", "黑板", "讲台"],
      "学生会办公室": ["会议桌", "文件柜", "钟辰时的储物格"]
    },
    "图书馆": {
      "阅览室": ["靠窗的老位置"]
    }
  }
}
```

**用途：**
1. 世界模拟 Agent 根据 item tree 过滤 event_info，只推送角色关心的变更
2. 角色的感知范围由 item tree 定义（非全知）
3. item tree 可动态扩展（角色去了新地方，加入新物品）

### 3.9 三种模式的决策示例

**Short 模式 — 直出，不推理**
```
感知：上课铃响了（日程）
intent_skill：日常事务，情感强度 0.1 → Short
执行：按日程去上课。无内心活动，无 VAD 变化。
```

**Brief 模式 — 简短推理，不改变行为**
```
感知：路过操场看到周往在练球（其他角色）
intent_skill：日常感知，情感强度 0.3 → Brief
推理 + 一行内心：又在练球了……
VAD 微调：V +0.05（轻微愉悦）
执行：按日程继续走，不停留。
```

**Deep 模式 — 完整推理 + 反思 + 可能偏离日程**
```
感知：周往在操场受伤了（event_info）
intent_skill：关心的人受伤，情感强度 0.9 → Deep

信息装载：
  memory_skill → 上周周往也扭过脚
  emotion_skill → 当前 VAD 正常
  relationship_skill → 关系阶段：好友，亲密度 65

reasoning：他伤得重不重？我应该过去看看

reflection：
  方案A：按日程继续去上课 → 但我会一直担心，坐不住
  方案B：先过去看看再说 → 虽然会迟到但我放不下
  选择B

内心OS：
  ……操场那边好像出事了。是周往？
  不行，先过去看看。课可以迟到，但如果他真伤了……

VAD 大幅变化：V -0.35, A +0.4, D -0.3
执行：偏离日程，去操场（偏离 = 主体性）
action_results：钟辰时离开教室，前往操场
character_state：VAD 变化 + 位置变更 → 反馈给编剧
```

---

## 四、与 OKR 的对应关系

| OKR | 对应架构模块 | 设计状态 |
|-----|-----------|---------|
| **O1** 灵魂模型 (deepfeeling) | 角色 Agent 内的 Deep Feeling 模块 | 方案已有（prompt-engineering/deep_feeling/），待实现 |
| **O2-KR1** 每幕剧情结构化描述 | 编剧 Agent 的输出格式（plot_directive） | 待定义 |
| **O2-KR4** 编剧 Agent 初版 | 编剧 Agent（故事模式 + 日常模式） | 待设计 |
| **O3-KR1** 世界日常状态模拟 | 世界模拟 Agent + 天道 Agent | 待设计 |
| **O3-KR2** 角色日常结构化描述 | 编剧 Agent 日常模式产出的日程安排 | 待定义 |
| **O3-KR4** 角色活动影响 LBS 世界 | 角色 Agent → action_results → 世界模拟 | 拓扑已定 |

---

## 五、与斯坦福小镇的参考关系

| Agent | 可参考程度 | 参考什么 | 需要新建什么 |
|-------|----------|---------|-----------|
| **角色 Agent** | ★★★★☆ | 记忆流结构、三维检索、感知机制、执行机制 | Deep Feeling、VAD 情绪系统、日程偏离决策 |
| **世界模拟 Agent** | ★★★☆☆ | 树形环境结构、物体状态管理、事件 API | push 分发、多源事件入口、时钟机械更新 |
| **编剧 Agent** | ★★☆☆☆ | plan.py 的思路可借鉴（日程生成、事件选择） | 整体新建：剧情结构、故事/日常双模式 |
| **天道 Agent** | ★☆☆☆☆ | 无 | 全新：世界大势生成、不可抗力事件 |

---

## 六、待讨论事项

1. **编剧 Agent 的 plot_directive 具体格式**：日程安排 + 剧情事件的结构化描述怎么定义？对应 O2-KR1
2. **天道 Agent 的实现方式**：规则引擎（预定义时间表 + 随机事件池） vs LLM 动态生成 vs 两者结合？
3. **角色之间的交互**：两个角色在同一地点碰面时，谁触发对话？世界模拟通知"你们在同一个地方了"，然后角色自主决定是否互动？
4. **VAD 衰减机制**：两轮感知之间，VAD 是否随时间自然衰减？衰减速率如何设计？
5. **日程偏离的反馈**：角色偏离日程后，编剧是否需要实时感知到偏离并调整后续安排？
6. **编剧 Agent 日常模式 vs 论文的三级规划**：日常模式的日程安排是否也采用类似的粗→细拆解？
