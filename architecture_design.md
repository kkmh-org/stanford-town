# NPC Agent 系统架构设计文档

> 2026-04-15 · 基于 generative_agents（斯坦福小镇）研究 + 团队战役二 OKR 讨论

---

## 一、系统总览

### 1.1 设计目标

让 AI 角色具备**灵魂感**（主体性 + 进化）：
- **有故事**：编剧 Agent 推动剧情，角色活在故事里
- **有主体**：角色通过 Deep Feeling 自主感受、推理、决策，有能力偏离编剧安排的轨道
- **有进化**：角色的情感、关系、认知随经历持续变化

### 1.2 核心组件

| 组件 | 角色 | 类比 |
|------|------|------|
| 时钟 | 驱动一切的心跳 | 时间 |
| 天道 Agent | 独立运行，产出不可抗力事件和世界大势 | 大运流年 / 自然法则 |
| 编剧 Agent | 产出有叙事意图的事件 + 角色日程 + 剧情指令 | 编剧 / 导演 |
| 世界模拟 Agent | 维护世界状态，分发变更通知 | 物理世界 |
| 角色 Agent | 感知、感受、决策、行动的主体 | 人 |

### 1.3 系统拓扑图

> 在线查看（含连线可视化）：[NPC Agent 系统拓扑图](https://aigc-image-1253543992.cos.ap-beijing.myqcloud.com/architecture/npc_agent_topology_20260415_114920.html)

```
  时钟 ──tick──→ 天道 ──common_events──→ 世界模拟 ──event_info──→ 角色
    │              └──context──→ 编剧                              ↑↓
    └──tick─────────────────→ 世界模拟    编剧 ──plot_directive──→ 角色
                                ↑          │                       │
                           角色行为结果    events               reply
                           用户操作        ↓                       ↓
                                      世界模拟 ──world_state──→ 编剧
                                                               用户
```

### 1.4 完整数据流清单

| # | 从 | 到 | 数据 | 说明 |
|---|----|----|------|------|
| 1 | 时钟 | 天道 | `tick` `current_time` | 驱动天道演进 |
| 2 | 时钟 | 世界模拟 | `tick` `current_time` | 驱动机械更新（课表、作息） |
| 3 | 天道 | 编剧 | `context` | 世界大势/氛围 |
| 4 | 天道 | 世界模拟 | `common_events[]` | 不可抗力事件（天气、灾害） |
| 5 | 编剧 | 世界模拟 | `common_events[]` `persona_events[]` | 有意图的世界事件 + 角色相关世界事件 |
| 6 | 编剧 | 角色 | `plot_directive` | 剧情指令 + 角色日程安排 |
| 7 | 世界模拟 | 编剧 | `world_state` | 当前物品状态，编剧决策依据 |
| 8 | 世界模拟 | 角色 | `event_info[]` | 按角色 item tree 过滤分发 |
| 9 | 角色 | 世界模拟 | `action_results[]` | 行为导致的物品状态变更 |
| 10 | 角色 | 编剧 | `character_state` | 情感/关系/位置/成长 |
| 11 | 用户 | 世界模拟 | `user_input` | 用户直接操作世界 |
| 12 | 用户 | 角色 | `user_message` | 用户跟角色聊天 |
| 13 | 角色 | 用户 | `reply` | 角色回复用户 |

---

## 二、各 Agent 详细设计

### 2.1 天道 Agent

独立运行，不受任何其他 Agent 影响。像自然法则一样，产出不可抗力的世界事件。

```
                ┌──────────┐
  tick ────────→│ 天道 Agent │
  current_time ─→│ (大运流年) │
                │          │
                │ 内部：     │
                │ 世界大势   │
                │ 自然规律   │
                │ 随机事件池 │
                └────┬─────┘
                     │
          ┌──────────┴──────────┐
          ▼                     ▼
  common_events[]           context
  (不可抗力事件)           (世界大势/氛围)
     ↓                        ↓
  世界模拟                    编剧
```

**context 示例：**
```json
{
  "current_phase": "篮球联赛备战期",
  "world_mood": "紧张、竞争",
  "season": "晚春、多雨",
  "active_macro_events": ["校际篮球联赛下周开始", "期中考试临近"]
}
```

### 2.2 编剧 Agent

有叙事意图的事件生产者。两种模式：故事模式和日常陪伴模式。

```
  天道.context ─────→┐
  world_state ──────→│
  character_states ─→│  编剧 Agent
  user_behavior ────→│  (故事/日常)
  plot_progress ────→│
  story_config ─────→│
                     └──────┬─────────────┬────────────┐
                            ▼             ▼            ▼
                     common_events   persona_events   plot_directive
                     (有意图的        (角色相关       (剧情指令
                      世界事件)        世界事件)       + 日程安排)
                        ↓                ↓               ↓
                     世界模拟          世界模拟           角色
```

**plot_directive 包含两类内容：**
1. **日程安排**（默认轨道）：角色今天的日程表，角色可以基于 deepfeeling 偏离
2. **剧情事件**：接下来要发生的剧情内容，角色通过感知获取

**两种模式的差异：**

| | 故事模式 | 日常陪伴模式 |
|---|---------|-----------|
| 世界观 | 角色的世界（南一高中） | 用户的真实世界 |
| 编剧产出 | 推动角色世界故事线的事件 | 角色"那边"发生了什么 |
| 用户角色 | 用户是世界中的参与者 | 用户是"另一个世界的人" |
| 交互方式 | 角色与用户在同一场景 | 聊天、打电话、发消息（异步） |

### 2.3 世界模拟 Agent

纯粹的状态管理器。不产出事件，只处理和分发。

```
  天道.common_events[] ────→┐
  编剧.common_events[] ───→│
  编剧.persona_events[] ──→│  世界模拟 Agent
  tick + current_time ─────→│  (状态管理器)
  角色.action_results[] ──→│
  用户.user_input ─────────→│
                            │
                    ┌───────┴────────┐
                    │  全量物品状态树  │
                    └───────┬────────┘
                            │
                 ┌──────────┴──────────┐
                 ▼                     ▼
            world_state           event_info[]
            (世界状态快照)         (按角色 item tree
               ↓                  过滤后分发)
              编剧                    ↓
                                    角色
```

**全量物品状态树示例：**
```json
{
  "南一高中": {
    "教学楼": {
      "高二三班教室": {
        "钟辰时的书桌": { "state": "整齐摆放着课本和笔记", "owner": "钟辰时" },
        "黑板": { "state": "写着今天的值日表", "owner": null }
      }
    }
  }
}
```

**event_info 示例：**
```json
{
  "item": "南一高中:教学楼:高二三班教室:钟辰时的书桌",
  "old_state": "整齐摆放着课本和笔记",
  "new_state": "课本散落一地，椅子倒了",
  "cause": "周往路过时不小心撞到",
  "time": "2026-04-15 10:15",
  "related_personas": ["钟辰时", "周往"]
}
```

### 2.4 角色 Agent

感知、感受、决策、行动的主体。详见第三章。

```
  event_info[] ────→┐
  plot_directive ──→│  角色 Agent
  user_message ────→│
                    │  ┌──────────────────────────┐
                    │  │ 感知 → 记忆+情绪 → 分流:   │
                    │  │                           │
                    │  │  Short → 直出按日程         │
                    │  │  Brief → 一行内心，按日程    │
                    │  │  Deep  → 完整推理，可偏离    │
                    │  │                           │
                    │  │ → 执行                     │
                    │  └──────────────────────────┘
                    │
           ┌────────┼────────┐
           ▼        ▼        ▼
    action_results  reply  character_state
      ↓               ↓        ↓
    世界模拟          用户      编剧
```

---

## 三、角色 Agent 内部架构

### 3.1 设计原则

1. **角色是"活人"**：主动感知环境，不是被动接收通知
2. **记忆和情绪是两套独立系统**：记忆 = 经历了什么；情绪 = 当下的感受（VAD）
3. **用 Deep Feeling 替代论文的反思模块**：不只是"想明白了什么"，而是"经历了什么样的内心过程"
4. **编剧给默认轨道，角色保留主体性**：日程由编剧安排，角色可以基于 deepfeeling 偏离
5. **偏离 = 主体性的体现**：灵魂感 = 主体性 + 进化

### 3.2 三种模式：Short / Brief / Deep

角色不是对所有感知都做深度推理。大部分时候直接按日程执行，不需要任何内心戏。

| 模式 | 触发条件 | 做什么 | 占比 | 举例 |
|------|---------|--------|------|------|
| **Short** | 日常琐事，无情感波动 | 不推理，直接按日程执行 | ~80% | 上课铃响→去上课 |
| **Brief** | 轻微情感触动 | 简短推理，一行内心，不改变行为 | ~15% | 看到周往打球→"又在练球了" |
| **Deep** | 强烈情感冲击 | 完整推理+反思+多方案权衡，可能偏离日程 | ~5% | 周往受伤→挣扎→偏离日程去看他 |

**只有 Deep 模式才可能触发日程偏离。**

### 3.3 内部流程

```
  感知到的所有信息
         │
         ▼
  ┌──────────────┐
  │ 记忆 Memory   │◄──互相影响──►┌──────────┐
  │ (观察/反思/计划) │              │ 情绪 VAD  │
  └──────┬───────┘              └────┬─────┘
         │                          │
         └────────────┬─────────────┘
                      ▼
              intent_skill
            意图 + 情感强度判断
                      │
         ┌────────────┼────────────┐
         ▼            ▼            ▼
     ┌───────┐   ┌────────┐   ┌────────┐
     │ Short │   │ Brief  │   │  Deep  │
     │       │   │        │   │        │
     │不推理  │   │一行内心  │   │完整推理 │
     │直出    │   │VAD微调  │   │+反思   │
     │       │   │        │   │+权衡   │
     │       │   │        │   │        │
     │按日程  │   │按日程   │   │可偏离  │
     └───┬───┘   └───┬────┘   └───┬────┘
         │           │            │
         └───────────┴────────────┘
                     │
                     ▼
                   执行
          ┌────────┼────────┐
          ▼        ▼        ▼
   action_results reply  char_state
     → 世界模拟    → 用户   → 编剧
```

### 3.4 每 tick 循环

```
每个 tick：

1. 感知（Perceive）
   主动扫描：日程、物品变化、剧情、其他角色、用户消息、环境

2. 记忆 + 情绪更新
   感知内容写入记忆流，同步更新 VAD

3. 模式分流（intent_skill）
   · 无情感波动 → Short：跳过推理，直接按日程执行
   · 轻微触动   → Brief：简短推理，一行内心，按日程执行
   · 强烈冲击   → Deep：完整推理 + 反思 + 多方案权衡

4. 执行（Execute）
   Short / Brief → 按日程执行
   Deep + 强情感 → 可能偏离日程（偏离 = 主体性）

5. 异步更新
   emotion_update → 写入新 VAD
   memory_write → 写入记忆摘要
```

### 3.5 与斯坦福小镇论文的关键差异

| 环节 | 论文（斯坦福小镇） | 我们的设计 |
|------|-----------------|----------|
| 感知 | pull，扫描附近 tile 的 events | pull，感知源更丰富（日程 + 物品变化 + 剧情 + 用户 + 其他角色 + 环境） |
| 记忆 | 记忆流（观察/反思/计划，8字段），是唯一系统 | **保留记忆流，但与情绪分离为两套独立系统** |
| 情绪 | 没有 | **新增 VAD 情绪系统**，独立存储，与记忆互相影响 |
| 推理/反思 | 简单 LLM 推理 + 重要性>150 触发反思 | **替换为 Deep Feeling**（Short/Brief/Deep 三模式） |
| 规划 | 角色自主三级规划（粗→小时→执行级） | **日程由编剧安排**，角色感知日程作为默认轨道 |
| 主体性 | 完全自主（但缺乏叙事控制） | **编剧给轨道 + 角色可偏离**，偏离由 deepfeeling 驱动 |

### 3.6 记忆系统（参考论文）

**单条记忆 8 字段（与论文一致）：** 记忆ID、自然语言描述、类型（观察/反思/计划）、创建时间戳、最近访问时间戳、重要性分数（1-10）、引用指针列表、嵌入向量

**三维检索：** Final Score = Normalized(Recency) + Normalized(Importance) + Normalized(Relevance)

### 3.7 情绪系统（新增）

独立于记忆，用 **VAD 模型**表示角色当下情绪：V(愉悦度) / A(激活度) / D(支配感)，范围 -1~1。感知时同步更新，随时间自然衰减回基线。

### 3.8 角色 Item Tree

每个角色拥有关联物品的子集（世界全量树的子视图）。世界模拟 Agent 据此过滤 event_info，只推送角色关心的变更。item tree 可动态扩展。

```json
{
  "南一高中": {
    "教学楼": {
      "高二三班教室": ["钟辰时的书桌", "黑板", "讲台"],
      "学生会办公室": ["会议桌", "文件柜", "钟辰时的储物格"]
    },
    "图书馆": { "阅览室": ["靠窗的老位置"] }
  }
}
```

### 3.9 三种模式决策示例

**Short — 直出**
```
感知：上课铃响了 → 情感强度 0.1 → Short → 按日程去上课，无内心活动
```

**Brief — 一行内心**
```
感知：路过操场看到周往练球 → 情感强度 0.3 → Brief
内心：又在练球了……  VAD: V+0.05
按日程继续走
```

**Deep — 完整推理 + 可能偏离**
```
感知：周往在操场受伤了 → 情感强度 0.9 → Deep

信息装载：memory(上周也扭过脚) + emotion(当前正常) + relationship(好友,亲密度65)
reasoning：他伤得重不重？我应该过去看看
reflection：
  方案A：按日程上课 → 但我会一直担心
  方案B：先过去看看 → 虽然迟到但放不下 → 选B

内心OS：……操场那边好像出事了。是周往？
        不行，先过去看看。课可以迟到，但如果他真伤了……

VAD: V-0.35, A+0.4, D-0.3
执行：偏离日程，去操场（偏离 = 主体性）
→ action_results: 钟辰时离开教室前往操场
→ character_state: VAD变化 + 位置变更 → 反馈给编剧
```

---

## 四、与 OKR 的对应关系

| OKR | 对应架构模块 | 设计状态 |
|-----|-----------|---------|
| **O1** 灵魂模型 (deepfeeling) | 角色 Agent 的 Deep Feeling 模块 | 方案已有，待实现 |
| **O2-KR1** 每幕剧情结构化描述 | 编剧 Agent 的 plot_directive 格式 | 待定义 |
| **O2-KR4** 编剧 Agent 初版 | 编剧 Agent（故事 + 日常模式） | 待设计 |
| **O3-KR1** 世界日常状态模拟 | 世界模拟 Agent + 天道 Agent | 待设计 |
| **O3-KR2** 角色日常结构化描述 | 编剧日常模式产出的日程安排 | 待定义 |
| **O3-KR4** 角色活动影响 LBS | 角色 → action_results → 世界模拟 | 拓扑已定 |

---

## 五、与斯坦福小镇的参考关系

| Agent | 可参考程度 | 参考什么 | 需要新建什么 |
|-------|----------|---------|-----------|
| **角色** | ★★★★☆ | 记忆流、三维检索、感知、执行 | Deep Feeling、VAD、日程偏离 |
| **世界模拟** | ★★★☆☆ | 树形结构、物体状态、事件 API | push 分发、多源入口 |
| **编剧** | ★★☆☆☆ | plan.py 思路可借鉴 | 整体新建 |
| **天道** | ★☆☆☆☆ | 无 | 全新 |

---

## 六、待讨论事项

1. **编剧 Agent 的 plot_directive 具体格式**：日程安排 + 剧情事件的结构化描述怎么定义？
2. **天道 Agent 的实现方式**：规则引擎 vs LLM 动态生成 vs 两者结合？
3. **角色之间的交互**：两个角色碰面时，谁触发对话？
4. **VAD 衰减机制**：衰减速率如何设计？
5. **日程偏离的反馈**：角色偏离后，编剧是否实时感知并调整后续安排？
6. **编剧日常模式的日程粒度**：是否也采用类似论文的粗→细拆解？
